# homebrew-tap

Homebrew formulae for my projects.

## Installation

```bash
brew tap unvalley/tap
```

## Formulae

### rt

Interactive task runner that abstracts away various task runner file formats (Justfile, Taskfile, Makefile, etc.).

```bash
brew install unvalley/tap/rt
```

Or after tapping:

```bash
brew install rt
```
